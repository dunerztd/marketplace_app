<%# NavBar %>
<nav class="navbar navbar-expand-lg navbar-light bg-light">
      <a class="navbar-brand" href="/">
        Guitar Lessons
      </a>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse justify-content-end" id="navbarSupportedContent">

      <%# Show navbar items when user is logged-in %>
      <% if user_signed_in? %>
        <div class="navbar-nav">
          <%= link_to "Log Out", destroy_user_session_path, method: :delete, class: "nav-item nav-link mx-3" %>
        </div>        
	    <% else %>

      <%# Show navbar items when user is logged out %>
        <div class="navbar-nav">
          <%= link_to 'Login', new_user_session_path, class: "nav-item nav-link mx-3" %>
        </div>
        <div class="navbar-nav">
			    <%= link_to 'Sign Up', new_user_registration_path, class: "nav-item nav-link mx-3" %>
        </div>
      <% end %>
      </div>
    </nav>
